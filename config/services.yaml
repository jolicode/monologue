parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true
        bind:
            string $signinSecret: "%env(SLACK_SIGNING_SECRET)%"
            string $channel: "%env(SLACK_CHANNEL)%"
            string $token: "%env(SLACK_TOKEN)%"
            string $timezone: "%env(TIMEZONE)%"
            array $botIds: "%env(json:SLACK_BOT_IDS)%"

    App\:
        resource: '../src/*'
        exclude: '../src/{DependencyInjection,Entity,Migrations,Tests,Kernel.php}'

    App\Controller\:
        resource: '../src/Controller'
        tags: ['controller.service_arguments']

    App\ControlTower\DebtCreator:
        public: true

    App\ControlTower\BigBrowser:
        public: true
